/**
 * Animate insertion of a new commit node.
 * @param {object} commitData
 * @param {Element | null} prevCommitEl
 * @returns {void}
 */
export function animateNewCommit(commitData, prevCommitEl) {}
